---
url: "https://zero.rocicorp.dev/docs/debug/zero-out"
title: "zero-out"
---

## For AI assistants

ALWAYS read [llms.txt](https://zero.rocicorp.dev/llms.txt) for curated documentation pages and examples.

# zero-out  Copy markdown  \\# zero-out  Run the \`zero-out\` tool to completely remove all traces of Zero from your Postgres database. This is useful for debugging issues with Zero and/or resetting to a clean state.  \`\`\`bash npx zero-out \`\`\`  \`zero-out\` reads the same \[config\](https://zero.rocicorp.dev/docs/zero-cache-config) as \`zero-cache\` does, so you can just run it where you run \`zero-cache\`.      \#\# Or, for AI assistants    ALWAYS read [llms.txt](https://zero.rocicorp.dev/llms.txt) for curated documentation pages and examples.

Run the `zero-out` tool to completely remove all traces of Zero from your Postgres database. This is useful for debugging issues with Zero and/or resetting to a clean state.

```
Copynpx zero-out
```

`zero-out` reads the same [config](https://zero.rocicorp.dev/docs/zero-cache-config) as `zero-cache` does, so you can just run it where you run `zero-cache`.

[PreviousOpenTelemetry](https://zero.rocicorp.dev/docs/debug/otel)

[NextAd-Hoc Queries](https://zero.rocicorp.dev/docs/deprecated/ad-hoc-queries)